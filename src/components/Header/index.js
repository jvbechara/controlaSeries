import React from 'react';
import "./styles.css";
import Menu from '../Menu';

const Header = () => (
    <div className="menu-header">
        <header>  
            <Menu />
        </header>
    </div>
);

export default Header;